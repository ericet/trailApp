version: '3'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: backend-image
    container_name: backend-container
    ports:
      - "5000:5000"

volumes:
  backend-data:
